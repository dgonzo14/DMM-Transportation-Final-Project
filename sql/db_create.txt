CREATE DATABASE IF NOT EXISTS TRANSIT_WEATHER_DB;
CREATE WAREHOUSE IF NOT EXISTS TRANSIT_WH WITH WAREHOUSE_SIZE = 'XSMALL' AUTO_SUSPEND = 60;

USE DATABASE TRANSIT_WEATHER_DB;
CREATE SCHEMA IF NOT EXISTS RAW;        -- For initial uploads
CREATE SCHEMA IF NOT EXISTS CLEAN;      -- For joined and normalized data
CREATE SCHEMA IF NOT EXISTS ANALYTICS;  -- For final report tables
